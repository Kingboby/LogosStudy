Logos Study App:

To run the app in codespaces, a base database and .env have beem provided for testing purposes. 
Login with user: "test", password: "testpassword"
This user has populated data, study sessions and goals to fully review the PWA.
This user also has a demo, .ics calendar link saved.
There is also demo items in the library.

NOTE:
For ease of demo, the app falsl back to a local SQLite database placeholder and secret key if no .env is present, so it runs without any configuration. In a production deployment these would be hidden environment variables.
